# CV6 G5 Shared product independent gate

> Task: `T0-CV1 / Shared canonical Canvas V1 activation`
>
> Final integrated product head: `673b2bc00aa81079d5c7c60059d71ee9b5e483fe`
>
> CV6 recommendation: `ACCEPT / READY_FOR_GATE`

This is an independent QA recommendation. It does not edit the Master Plan or
contracts and does not declare G5 accepted; CV0 owns the Gate decision. The
external three-service browser Golden Path is intentionally deferred to G6.

## Isolation and merge provenance

```text
worktree: /Users/docfat/.codex/worktrees/t0-cv1-cv6-g5-shared-gate
branch:   codex/t0-cv1-cv6-g5-shared-gate
baseline: 6e7fd7f73483bfd4061cfb26b4dff5a7f03c4cd7
```

CV6 used normal merges only. No rebase, reset, cherry-pick or manual product
copy was used. The final integration merge and exact parents are:

```text
37a8b850fbba25c301bce8eb9b389a8a2fdf99a5
  5fd23240d4213547e19c12ffd87cfda8c13e1d19
  673b2bc00aa81079d5c7c60059d71ee9b5e483fe
```

The earlier Shared product merge was:

```text
698c157a188c472fefb169d684c97e1926555b14
  123e321a8185308f155df9e80aeaa144d35b8e9a
  8c5f0cf7f467d6892252ed384175f19a765b8f60
```

Merge diffs are upstream integration content and are excluded from the CV6
write set. The frozen CV5 product commit is
`07ef24e7479957f5474e9237601ba9a032e0e9b2` with RED parent
`4c03aca63141f91561ac8560f50f32b14f9daacf`.

## Independent Shared product evidence

```bash
node scripts/t0-canvas-v1-gate/run-shared-g5-gate.mjs
```

```text
Activation Transport/0.1 25-vector validator:  6/6 PASS
CV6 Shared static/public/security policy:       7/7 PASS
CV6 Shared public runtime harness:              7/7 PASS
External-browser harness policy only:           3/3 PASS
Historical exact 4-RED product attestation:     1/1 PASS
SHARED_G5_GATE_PASS
```

The historical attestation executes the four originally frozen suites against
the real Shared product and proves their exact aggregate `37/37` result with no
skip or todo. It does not substitute a mock or mark an expected RED green.

The independent static and runtime gates establish:

- the canonical route requires explicit lowercase UUID `projectId` and one
  exact `packageId` query value; missing, duplicate, unknown or malformed query
  state fails closed without a default Package;
- `activationAttemptId` is generated by browser crypto and remains page-memory
  state; it is absent from URL, DOM, Web Storage and console output;
- activation, legacy open, formal bootstrap/workspace reads, approval prepare,
  command dispatch and workspace refresh use relative same-origin requests;
- the HTTP and Bridge parsers exact-check Origin/session/scope/package and safe
  response shapes, and never project internal tokens, URLs or provider facts;
- approval preparation obtains a fresh CSRF value internally and high-cost
  dispatch preserves the exact host-approved command identity and approval;
- refresh is fail-closed and ordered workspace, document, assets, then
  readiness for each workspace shot; no fixture/default hydration is used;
- Pilot `production-canvas` is the sole route branch returning
  `CanvasV1RouteContainer`; DemoRouter's legacy page cannot service that route;
- the two dev-proxy flags require valid loopback URLs and retain
  `changeOrigin: false`;
- the external Playwright harness is policy-checked for exact canonical URLs,
  1440x900 and 1672x941 viewports, storage/console/network forbidden facts and
  dynamic approval equality, but the browser run itself was not started.

## Contract and cross-service regression evidence

```text
CV5 Shared owner exact matrix:                  67/67 PASS (5 files)
root full unit/integration matrix:              521/521 PASS (56 files)
G5 additive schema/fixture/security groups:     9 + 6 + 7 PASS
Story owner parser parity groups:               7 + 11 PASS
browser parser and real page hydration:         6/6 PASS (2 files)
G5_ADDITIVE_CONTRACT_GATE_PASS
Control activation/materialization/authority:   33/33 PASS (8 files)
Story formal workspace key interfaces:          29/29 PASS (10 files)
Story independent product policy/runtime:       6/6 + 10/10 PASS
Workspace/materialization contracts:            69 vectors / 9 groups PASS
Workspace Authority contract:                   30 vectors / 6 groups PASS
root TypeScript/Vite build:                     PASS
Control build and strict typecheck:              PASS
Story backend/Electron build:                   PASS
governance:                                     PASS
git diff --check and cached diff check:          PASS
```

The first Story key-interface invocation was made from the repository root and
failed only because that invocation did not load Story's `@/` TypeScript path
aliases. CV6 reran the identical ten files from `apps/storycanvas`; the result
was `29/29 PASS`. This is classified as a QA command-environment error, not a
product failure.

The Story build regenerates the tracked serve bundle. After its successful
build, CV6 restored that one QA-generated artifact to exact HEAD content. No
generated product diff is included in the handoff commit.

## Protected and write-set evidence

Comparison of the final QA merge tree with integration
`673b2bc00aa81079d5c7c60059d71ee9b5e483fe` was empty before this handoff.
`git diff --check` passes. The protected
`apps/storycanvas/data/vendor/byteplus.ts` is not tracked on the QA branch and
has no diff across the final integration merge. In the original workspace it
remains the pre-existing untracked zero-byte file with SHA-256:

```text
e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855
```

CV6 did not modify product, Master, contracts, BytePlus or historical Shared
RED files outside the allowed test/script/handoff set.

## Exact CV6 commits and paths

```text
fd9a8cc9b7c925ce9344dc706e6a544fed10465c  freeze Shared G5 activation RED
8f655087937fcf284787f871a95ceb6c76828fb2  prepare external browser gate
123e321a8185308f155df9e80aeaa144d35b8e9a  narrow public surface assertions
5fd23240d4213547e19c12ffd87cfda8c13e1d19  align runtime fixtures
```

Exact CV6 paths across those commits and this handoff:

```text
scripts/t0-canvas-v1-gate/playwright.shared-g5.config.ts
scripts/t0-canvas-v1-gate/run-shared-g5-browser-gate.mjs
scripts/t0-canvas-v1-gate/run-shared-g5-gate.mjs
tests/e2e/canvas-v1/shared-g5-browser-policy.gate.mjs
tests/e2e/canvas-v1/shared-g5-browser.spec.ts
tests/e2e/canvas-v1/shared-g5-historical.gate.mjs
tests/e2e/canvas-v1/shared-g5-policy.fixture.json
tests/e2e/canvas-v1/shared-g5-policy.gate.mjs
tests/e2e/canvas-v1/shared-g5-runtime.gate.test.ts
docs/program/t0-canvas-v1/handoffs/CV6_G5_SHARED_PRODUCT_GATE.md
```

## Recommendation and non-claims

CV6 found no remaining G5 Shared product blocker and recommends `ACCEPT` for
G5 Business Integration with status `READY_FOR_GATE`. CV0 should verify the
commit objects, merge parents, commands and protected diff before recording
the Gate decision.

The three services were not started, no external browser screenshot was
captured, and no real or paid provider was called. This evidence therefore does
not claim G6 completion, browser Golden Path completion, AB Golden Path
implementation or Joint Gate acceptance.
