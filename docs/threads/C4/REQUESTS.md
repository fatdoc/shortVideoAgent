# C4 REQUESTS

> 跨模块请求写在这里。C0 决策后回填。

## RFC 模板

```
请求编号：REQ-C4-001
发起线程：C4
请求内容：
请求原因：
影响范围：
是否阻塞：是/否
临时方案：
C0 决策：待定
决策日期：
```

## 当前请求

无新增跨模块请求。

- 本轮未新增公共 shell/token 申请（只涉及本线程的样式与交互收敛，无需 C0 提供额外算力/密钥）。
- C0 仅需确认本次视觉对齐与验收标准是否通过。

说明：本线程评分/风险/Mock 生成均在 `src/components/script` 内前端派生，未改 `src/domain` / `src/mocks` / store API。
