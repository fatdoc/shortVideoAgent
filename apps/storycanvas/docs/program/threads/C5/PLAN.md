# C5 PLAN

## 已完成：首轮评审

1. 按权威 README 顺序读取共同记忆、角色边界、规则、工作台、双仓架构与集成合同。
2. 核验 StoryCanvas 工作树、基线、历史提交、固定 FireRed 版本和已有 Gate 成果。
3. 只读盘点脚本、画布、引用、连续性、任务、Provider、资产、时间线和导出。
4. 输出能力分类、最小适配层、三阶段计划、风险和 Requests。
5. 更新 C5 自有 STATUS、REQUESTS、CHANGELOG 和 HANDOFF。

## 已完成：D1 Adapter Wave

1. 精确复制六个 canonical 生产侧 fixture，登记 source suite digest，排除商业额度 fixture。
2. 实现 `ProjectProductionPackage v0.1` Schema、Mock grant scope、幂等接受/冲突、不可变快照与 rejected attempt。
3. 用 `sc_external_mappings` 投影项目、script-a、八镜、输入资产、任务、生成资产与导出物。
4. 复用现有结构化连续性表，为海底捞品牌、三里屯门店、会员权益和八镜建立契约；普通切镜不启用上一尾帧。
5. 实现确定性成功/失败 Provider、Receipt Outbox、同项目基础合并 `FALLBACK` 来源链。
6. 将画布主路径改为服务端 canonical bootstrap，展示 package/version/digest、Truth mode、C1—C8、规则和返回企业项目链接。

## 下一步

1. C6 接 SaaS 发包按钮与 StoryCanvas deep link。
2. C7 在解除运行禁令后执行 PKG/TOK/GTR/AST/XREP/TRUTH 与 16 步黄金路径。
3. C4/C5 冻结正式签名 grant、Outbox delivery/ack transport；D1 Mock handle 不升级为 credential。
4. 后续 Wave 再实现真实 Provider 字节、05 补拍回执、可播放 FFmpeg 输出与 CanvasVersion 保存。

## 继续禁止

- StoryCanvas 内的钱包、账本、客户价格、RateCard、Tenant/渠道域。
- 未批准的真实付费凭证和 FireRed 完整调用。
- 覆盖历史项目、重建画布或推翻结构化连续性。
