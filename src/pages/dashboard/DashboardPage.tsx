import { PagePlaceholder } from '../../components/common/PagePlaceholder';

export function DashboardPage() {
  return (
    <PagePlaceholder
      title="工作台 / 项目列表"
      owner="C2"
      description="展示项目列表、进度、负责人与进入 Brief 的入口。"
      route="/dashboard"
    />
  );
}
